# proj-init is now proj-config

This skill has been renamed. Use `/proj-config` instead.

If the user typed `/proj-init`, tell them: "This command has been renamed to `/proj-config`. Invoking it now."

Then immediately invoke `/proj-config`.
