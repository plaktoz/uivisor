appId: ${base}/integration
config: config.yml
# Shared helper: navigate to the integration page and assert core markers
# Used by run-flow.yaml via runFlow
commands:
  - goto: ${base}/integration
  - assertUrl: /integration
  - assertVisible:
      testId: int-runflow-marker
  - assertVisible:
      testId: int-goto-marker
